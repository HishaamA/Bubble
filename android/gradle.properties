# Bound Gradle's heap so local builds remain responsive on development machines.
org.gradle.jvmargs=-Xmx1536m

# Every native and Capacitor dependency in this project uses AndroidX.
android.useAndroidX=true
